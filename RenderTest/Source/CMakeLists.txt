INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/RenderTest/Header)

FILE(GLOB_RECURSE SOURCES *.cpp *.c)
FILE(GLOB_RECURSE HEADERS *.hpp *.h *.inl)

ADD_EXECUTABLE(RenderTest ${SOURCES} ${HEADERS})
TARGET_LINK_LIBRARIES(RenderTest glfw3 glew32 opengl32)